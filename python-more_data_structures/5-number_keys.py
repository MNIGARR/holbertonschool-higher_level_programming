#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    Returns the number of keys in a dictionary.
    Args:
        a_dictionary: The dictionary to count keys from.
    Returns:
        The number of keys (integer).
    """
    return len(a_dictionary)
